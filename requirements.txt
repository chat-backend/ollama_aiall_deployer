# ================================
#  Core Dependencies
# ================================
python-dotenv==1.0.1        # Load .env for app config
requests==2.32.3            # HTTP client for internal/external APIs

# ================================
#  Web Framework (FastAPI stack)
# ================================
fastapi==0.111.0            # Main API framework
uvicorn[standard]==0.30.0   # ASGI server (run behind Nginx)

# ================================
#  JSON / Serialization
# ================================
ujson==5.10.0               # Faster JSON parsing (optional but recommended)

# ================================
#  Security / Tokens
# ================================
pyjwt==2.8.0                # JWT handling for API auth

# ================================
#  Async / Performance (optional)
# ================================
gevent==24.2.1              # Optional: async worker class for background tasks
greenlet==3.0.3

# ================================
#  Utilities
# ================================
psutil==6.0.0               # System monitoring (CPU, RAM)
